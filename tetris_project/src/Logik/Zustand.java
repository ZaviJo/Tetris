package Logik;

public enum Zustand {
	/*Auch hier wird eine Enum-Klasse verwendet. Dies wird benötigt, da das Game in verschiedenen Zuständen sein kann: start, ingame, pause, verloren. 
	 */
	start, ingame, pause, verloren
}
